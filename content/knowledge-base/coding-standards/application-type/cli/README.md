# CLI Application Type

This folder exists so `cli` can be discovered as an application type.

Keep generic CLI rules out of this folder unless they are reusable across many languages.

For TypeScript CLI rules used in this repo shape, prefer:

- `coding-standards/conditional/typescript-cli.md`
